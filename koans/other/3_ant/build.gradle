/*
 * INSTRUCTION:
 * ===========
 */

// ------------ START EDITING HERE ----------------------
task zip << {
    ant.zip(destfile: 'archive.zip') {
        fileset(dir: 'toZip') {
            exclude(name: '**.secret')
        }
    }
    ant.checksum(file: 'archive.zip')
}
// ------------ STOP EDITING HERE  ----------------------

